# Experiments

Controlled, recorded experiments that do not modify production code.

Each experiment should record hypothesis, method, inputs, outputs, evidence
class, and whether it may be replayed without touching golden reference files.
